// module for sandbox view
angular.module('bootCamp.sandbox', [
	// add any factory dependencies here

])

// if there are factory dependencies, add function it depends on as a function argument
.controller('SandboxController', function($scope) {
	console.log("inside SandboxController---------------");
})