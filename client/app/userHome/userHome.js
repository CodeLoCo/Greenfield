// module for userHome view
angular.module('bootCamp.userHome', [
	// add any factory dependencies here

])

// if there are factory dependencies, add function it depends on as a function argument
.controller('UserHomeController', function($scope) {
	console.log("inside UserHomeController---------------");
})